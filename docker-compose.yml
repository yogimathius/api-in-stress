version: "3"

services:
  api-gateway:
    build: .
    deploy:
      replicas: 2
      resources:
        limits:
          cpus: "0.5"
          memory: "1024M"
    environment:
      DATABASE_URL: postgresql://postgres:pass123@postgres:5432/warriors
    ports:
      - "3000"
    networks:
      - mynetwork

  nginx:
    image: nginx:1-alpine
    volumes:
      - ./nginx.conf:/etc/nginx/nginx.conf:ro
    ports:
      - "4000:4000"
    networks:
      - mynetwork
    depends_on:
      - api-gateway

networks:
  mynetwork:
